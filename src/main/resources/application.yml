server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_ADDRESS}/fsmusic?characterEncoding=UTF8&userSSL=false
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}